UnitOnlyEntrypointGuard::constructor
└── when passing valid parameters
    ├── it sets entrypoint address
    ├── it sets emergency caller address
    └── it sets multiSendCallOnly address


UnitOnlyEntrypointGuard::checkTransaction
├── when caller is entrypoint
│   ├── when operation is entrypoint call
│   │   └── it allows transaction
│   └── when operation is entrypoint delegate call
│       ├── when target is entrypoint multiSendCallOnly
│       │   └── it allows transaction
│       └── when target is not entrypoint multiSendCallOnly
│           └── it reverts with UnauthorizedDelegateCall
├── when caller is emergency caller
│   ├── when operation is emergency call
│   │   └── it allows transaction
│   └── when operation is emergency delegate call
│       ├── when target is emergency multiSendCallOnly
│       │   └── it allows transaction
│       └── when target is not emergency multiSendCallOnly
│           └── it reverts with UnauthorizedDelegateCall
└── when caller is not entrypoint or emergency caller
    └── it reverts with UnauthorizedSender
